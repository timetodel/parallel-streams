# Wave plans

Plans that get split into streams live here — one file per wave, named `YYYY-MM-DD-<topic>.md`.

The folder is named in `.parallel-streams.md`, so the coordination channel's nudge guard watches it:
editing a plan here reminds you that an addition to the plan never reaches a neighbour's running
session on its own, and shows the command that does reach it.
